<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, label: "Title" %>
  <%= f.input :technologies_used, hint: "HTML, Rails, etc" %>
  <%= f.error :base %>
  <%= f.button :submit %>
<% end %>
