<% if policy(@comment).approve? %>
  <%= form_for @comment, remote: true, url: {method: "patch"} do |f| %>
    <% f.hidden_field :approved, value: true %>
    <%= f.submit "Approved" %>
  <% end %>
<% end %>
