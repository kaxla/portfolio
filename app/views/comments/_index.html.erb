
<table>
  <thead>
    <tr>
      <th>Comments</th>
    </tr>
  </thead>

  <tbody>
    <% @post.comments.each do |comment| %>
      <tr id ="<%= dom_id(comment) %>">
        <td><%= comment.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
