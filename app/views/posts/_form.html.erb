<%= simple_form_for @post, remote: true do |f| %>
  <%= f.error_notification %>
  <%= f.error :base %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.button :submit %>

<!--Publish box-->
  <% if current_user.editor? %>
    <%= f.input :published %>
  <% end %>
<% end %>

